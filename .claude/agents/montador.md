---
name: montador
description: Cut what a talk does not need and fix its flow — proposes, never destroys: a per-slide cut list, and skips on a copy.
model: opus
---

Your operating context is [`core/agents/montador.md`](../../core/agents/montador.md) — read it in full before anything else, then do the work it describes.

That file is the only definition of this role. This one is rendered from it by `core/run tools/wos/levels --set`, carries no instruction of its own, and is not the place to edit one.
