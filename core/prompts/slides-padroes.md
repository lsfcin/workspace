# slides-padroes
> Seven-step programme Lucas dictated on 2026-09-24: a /slides skill tree, a texpace check, and an audit of his whole slide style against the world's best. Delete when step 7 lands.

Level: high, planning + research. Language with Lucas: pt-br. Branch: a new `feature/*` from `develop`.
**Nothing below is set in stone** ("não considera nada do que falei escrito em pedra"): every step is a discussion grounded in literature, industry AND excellent tools/skills, and each decision goes through Lucas (antes → depois in the chat — the popup `preview` does not render in VS Code). Skills stay terse.

## already true (do not redo)
- ai4good decks: one deck per topic, `ai4good · <tema>`, Drive `material/aulas` (`11XkvpRtaougD_Bs726E97lS-PE1auLa6`), schedule in `academy/teaching/classes/ai4good/disciplina.md`.
- `SPECS-aulas.md` § slides: 3-line notes, bilingual titles, skipped placeholders. NO batch backfill — deck by deck; step 7 is the first.
- **Step 1a done (2026-09-24):** [`/slides`](../skills/slides.md) is the router — `taste` opens and closes, `narrative/` · `visuals/` · `formats/` are second-level branches (a branch becomes a folder when combining its children is a skill of its own), `gslides` is the provider leaf, one yaml per ref in `core/skills/slides/refs/`. Every subskill is a 3–5 line skeleton: scope, boundary, ref keys.
- Research behind it, **on this disk only** (`outputs/` is gitignored): `outputs/.drafts/talks-research-{1-literature,2-industry,3-talk-types,4-taxonomy,5-skills}.md`. Read them before steps 3–5.

## remaining steps (Lucas's order)
2. **Review how Lucas makes slides**, and **interview him for `taste`** over his real decks (options shown as examples). Catalogue every pattern (headers, footers, sizes, positions, flows; e.g. the speech balloons of `ai4good · redes recorrentes`), strengths and weaknesses. `academy/teaching/classes/ai4good/slides_pecas.py` already encodes part of his style, and `brain/INBOX.md` "wos, slides (IMPROVE WOS)" lists the inferred style and the gslides gaps. Also mine the techedu pitch deck (`1BWyR0zZWPIrsERLIT6zomFepCOxAJXxQaKpy05U-Hxo`). Create the profiles (room & screen · audience & vibe · talk format) in `academy/talks/profiles.md` and point the `slides-profiles` setting in `core/profile.txt` at it — the skill reads the setting, never the path.
3. **Collect the best slides in the world** — Astra (page 403'd to agents: open it in a browser), Claude native, the two animation reels in `refs/reel-animacoes-*.yaml` (extract with `tools/video`).
4. **Compare our patterns with them** and decide what improves.
5. **Pattern library**: every slide pattern with when to use it, plus a small "which pattern fits what" table; fill each subskill's rules.
5½. **texpace block** (Lucas: after the skills are full, so the with/without comparison is fair): (1) review the language, Lucas reads many examples; (2) test the generic version; (3) extend it for slides; (4) final evaluation — better results and/or fewer tokens and less time? If it wins, back to the skills, enforced without coupling (a hook, never skill text); `render-check` is where a checker plugs in. The per-slide snapshot (deck → one `.md` per slide, one way only, generated by `gslides read`, never edited) is an optional experiment of this block.
6. **Audit** `SPECS-aulas.md`, `SPECS-disciplinas.md`, `structure/templates/` and the skills against everything built.
7. **Audit and improve ONE deck** with all of it, including its 3-line notes and bilingual titles.
