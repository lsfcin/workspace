# render-check
> Render → critique → fix, at most two rounds: overlap, overflow, off-grid, small images.

`lint` first (zero tokens: small text, off the slide, over the logo), then `preview` for what only an eye sees. Checks, never designs. A future texpace checker would plug in here, optional.

Refs: `pptx-skill` · `slide-self-verify`
