# animation
> Click-by-click builds that show change; in Google Slides a build is a slide sequence (the API has no animation).

Only when the change itself is the content; rendered motion is `video`.

Refs: `berney2016` · `tversky2002` · `remotion` · `reel-animacoes-1` · `reel-animacoes-2`
