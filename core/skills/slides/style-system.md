# style-system
> Lock colour, font, size and spacing tokens before any content, and hold every slide to them.

Tokens are decided here; each `visuals/` child applies them.

Refs: `theme-factory` · `grid-8pt`
