# Files this workspace GENERATES. Excluded from the line cap, the fanout signal and the size
# dashboard — an authoring rule aimed at a file nobody authored produces a finding nobody can act
# on, and the only honest fix would be to make the generator write worse output.
#
# NOT vendored.txt, and the difference is the whole reason this file exists: that list means
# "third party, not ours to split", this one means "ours, but written by a tool". Filing our own
# output as somebody else's would be a lie that costs nothing today and misleads every later
# reader of both lists.
#
# Same contract as vendored.txt and gitignore-exceptions.txt: a NAMED, reviewed list, never a
# heuristic. A line here is a claim that a tool in this repo writes the file and that editing it
# by hand is a mistake. Every entry names its generator.
#
# fnmatch globs, workspace-relative.

# The workspace picture — core/tools/wos/diagram/architecture, regenerated by /roundup.
# The per-project glob is here because per-project documents are already planned (ROADMAP);
# until it lands the glob matches nothing, which costs nothing.
ARCHITECTURE.html
*/ARCHITECTURE.html

# The skill mirrors are deliberately NOT here, and since 2026-08-29 they no longer need to be:
# git does not track them at all (.gitignore § the published units). The reason a glob here could
# never have worked still holds and is worth keeping — every pattern in this file is matched against
# a RESOLVED path, so while they were symlinks `.claude/skills/inbox/SKILL.md` arrived as
# `core/skills/inbox.md` and no glob written here could match it. The predicate that works on the
# unresolved path is entropy_corpus.is_generated_mirror, which three checks already use, and the
# size signal now uses it too.

# Research artifacts — core/flows/research/*, every one of which declares `outputs/<slug>-*.md`
# as the file it writes. A peer review, an audit or a provenance record is the flow's product, and
# holding it to an authoring rule has exactly one honest fix: make the flow write worse output.
# The CONTEXT.md routing each of these directories is authored, and is matched here too — the only
# rule this waives is the size cap, which a router never approaches.
*/outputs/*
outputs/*

# Every ISSUES.md, at the root and in each code repo. The pattern used to be anchored to the root
# one, on the reasoning that every other ISSUES.md was hand-written from its first line to its last;
# the entropy scatter (ruled 2026-08-20) ended that — each code repo's ledger now carries the same
# generated entropy block the root does, holding its own findings.
#
# The root additionally carries the verify block (core/tools/wos/roundup). Together these are
# hundreds of lines of generated prose that no split could improve, so the cap is waived for the
# whole file.
#
# These are the one HALF-authored kind of entry in this list, and that is a real cost rather than a
# detail: the hand-written issues above the blocks lose the size signal too. Accepted because the
# alternative is a per-block cap — a second numeric law, answering a question BLOCK_LINES already
# answers, which is the drift file_law.py exists to prevent. If a hand-written half ever grows past
# a screen, it shards like any other type and the shards are ordinary capped files.
ISSUES.md
*/ISSUES.md
